/** @type {import("prettier").Config} */
const config = {
  plugins: [
    'prettier-plugin-astro',
  ],
  // Optional: Add other global Prettier rules here if needed
  // semi: true,
  // singleQuote: true,
};

module.exports = config;